<h1><%= I18n.t('title.event.edit') %></h1>

<div class="row">
  <div class="col-md-6 buffer-top">
    <%= render 'form' %><br/>
    <%= link_to I18n.t('buttons.delete_event'), event_path(@event), method: :delete,
                data: { confirm: I18n.t('events.event.destroy_confirm') }, class: 'btn btn-danger' %>
  </div>
</div>
