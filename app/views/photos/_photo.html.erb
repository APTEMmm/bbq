<%= link_to (image_tag photo.photo.variant(resize_to_fill: [150, 150])),
            url_for(photo.photo),
            data: { lightbox: 'photo-collection',
                    title: (render partial: 'photos/photos_collection', locals: { photo: photo }) } %>
