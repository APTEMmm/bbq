<div class="event-photo buffer-both">

  <%= link_to image_tag(photo.photo.variant(resize: "150x150")), photo.photo %>

  <%= I18n.t('words.added') %> <%= render partial: 'users/user', object: photo.user %>
</div>